# OpenWrt on S905X4 (SC2) - HK1 RBOX X4 1000M
# Processed by the Devmfc bootscript (uImage-wrapped, loaded via cfgload).
# boot.config is imported first (raw values) and processed last (final override).
box=s905x4_generic_gigabit
majorbranch=6.x.y
branch=6.6.y
release=6.6.157+
variant=mainline
os_img=zImage
dtb_img=dtb/amlogic/meson-sc2-s905x4-gbit.dtb
rd_img=
